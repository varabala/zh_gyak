<Page
    x:Class="LaptopConfigurator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LaptopConfigurator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<!-- Laptopok listája -->
		<ListView Grid.Column="0"
                  ItemsSource="{x:Bind DataModel.Laptops}"
                  SelectedItem="{x:Bind DataModel.SelectedLaptop, Mode=TwoWay}">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="local:Notebook">
					<StackPanel>
						<TextBlock Text="{x:Bind Brand}"/>
						<!-- További adatok megjelenítése -->
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
		
		

		<!-- Szerkesztő rész -->
		<StackPanel Grid.Column="1">
			<TextBox Header="Márka" Text="{x:Bind DataModel.SelectedLaptop.Brand, Mode=TwoWay}"/>
			<TextBox Header="Memória" Text="{x:Bind DataModel.SelectedLaptop.Memory, Mode=TwoWay}"/>
			<!-- További szerkesztési lehetőségek -->
		</StackPanel>
	</Grid>
</Page>
